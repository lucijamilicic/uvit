<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ispiti</title>
</head>
<body>
    <% if(exams.length == 0) { %>
        <p>Nema polozenih ispita</p>
    <% } else {%>
        <h3>Polozeni ispiti za studenta: <%= exams[0].student.name %> <%= exams[0].student.surname %></h3>
        <% for(let e of exams) { %>
        <p> <%= e.subject %> : <%= e.grade %> </p>
        <% }%>
    <% }%>
</body>
</html>